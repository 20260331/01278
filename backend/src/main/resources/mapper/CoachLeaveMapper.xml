<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fitness.mapper.CoachLeaveMapper">

    <select id="selectPageWithDetail" resultType="com.fitness.entity.CoachLeave">
        SELECT cl.*, c.name as coachName, co.name as courseName
        FROM coach_leave cl
        LEFT JOIN coach c ON cl.coach_id = c.id
        LEFT JOIN course co ON cl.course_id = co.id
        <where>
            <if test="coachId != null">
                AND cl.coach_id = #{coachId}
            </if>
            <if test="status != null">
                AND cl.status = #{status}
            </if>
        </where>
        ORDER BY cl.create_time DESC
    </select>

</mapper>
