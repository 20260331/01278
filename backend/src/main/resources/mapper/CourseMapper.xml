<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fitness.mapper.CourseMapper">

    <select id="selectPageWithCoach" resultType="com.fitness.entity.Course">
        SELECT c.*, co.name as coachName
        FROM course c
        LEFT JOIN coach co ON c.coach_id = co.id
        <where>
            <if test="name != null and name != ''">
                AND c.name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="type != null and type != ''">
                AND c.type = #{type}
            </if>
            <if test="coachId != null">
                AND c.coach_id = #{coachId}
            </if>
            <if test="status != null">
                AND c.status = #{status}
            </if>
        </where>
        ORDER BY c.start_time DESC
    </select>

</mapper>
