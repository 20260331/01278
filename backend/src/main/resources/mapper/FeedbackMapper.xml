<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fitness.mapper.FeedbackMapper">

    <select id="selectPageWithMember" resultType="com.fitness.entity.Feedback">
        SELECT f.*, m.name as memberName
        FROM feedback f
        LEFT JOIN member m ON f.member_id = m.id
        <where>
            <if test="memberId != null">
                AND f.member_id = #{memberId}
            </if>
            <if test="status != null">
                AND f.status = #{status}
            </if>
        </where>
        ORDER BY f.create_time DESC
    </select>

</mapper>
